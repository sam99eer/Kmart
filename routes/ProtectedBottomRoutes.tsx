import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const ProtectedBottomRoutes = () => {
    return (
        <View>
            <Text>ProtectedBottomRoutes</Text>
        </View>
    );
};

export default ProtectedBottomRoutes;

const styles = StyleSheet.create({});
