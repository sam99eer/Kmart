import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const ProtectedStackRoutes = () => {
    return (
        <View>
            <Text>ProtectedStackRoutes</Text>
        </View>
    );
};

export default ProtectedStackRoutes;

const styles = StyleSheet.create({});
